# Copy to .env (git-ignored) and fill in your real keys.
#   cp .env.example .env
# The variable NAME here must match `api_key_env` in dugalaxy.config.yaml.

OPENAI_API_KEY=sk-...your-key-here...
# ANTHROPIC_API_KEY=sk-ant-...
# Ollama needs no key (local).
